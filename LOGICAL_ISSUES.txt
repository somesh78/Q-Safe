================================================================================
Q_SAFE - LOGICAL ISSUES REPORT
Generated: January 21, 2026
Development Phase - Functional Issues Only (UI/UX Ignored)
================================================================================

================================================================================
CRITICAL LOGICAL ISSUES
================================================================================

1. AUTHENTICATION/AUTHORIZATION BROKEN
   Location: backend/transfers/views.py (Lines 33-51)
   Issue: signup endpoint requires IsAuthenticated permission
   Impact: Users cannot sign up because they need to be logged in first
   Fix Required: Change @permission_classes([IsAuthenticated]) to @permission_classes([AllowAny])

2. DOWNLOAD ROUTE INCORRECTLY PROTECTED
   Location: frontend/src/App.js (Lines 30-43)
   Issue: /download/:token route requires authentication
   Impact: Public download links won't work - anyone with link should access
   Fix Required: Move download route outside authentication check

3. PASSWORD VALIDATION MISSING
   Location: frontend/src/pages/Home.jsx (Lines 28-30)
   Issue: Password check shows alert but doesn't prevent upload from proceeding
   Impact: Files can be uploaded without passwords
   Fix Required: Add return statement after alert or disable upload button

4. NO PASSWORD STRENGTH REQUIREMENTS
   Location: All password inputs (Signup, Login, Upload)
   Issue: No minimum length, complexity, or confirmation validation
   Impact: Weak passwords accepted, typos not caught
   Fix Required: Add password validation logic

5. FILE UPLOAD RACE CONDITION
   Location: frontend/src/pages/Home.jsx (Lines 39-58)
   Issue: Upload can proceed even if password check fails
   Impact: Files uploaded with empty passwords
   Fix Required: Add early return in handleFileUpload if !password

6. TOKEN EXPIRY DOUBLE-CHECK CONFLICT
   Location: backend/transfers/views.py (Lines 237-241)
   Issue: Both signed token (3600s) and database expires_at checked
   Impact: Inconsistent expiry behavior, one might expire before other
   Fix Required: Use single source of truth for expiry

7. FAILED ATTEMPT COUNTER DEADLOCK
   Location: backend/transfers/views.py (Lines 268-273)
   Issue: Failed attempts only reset on success, but locked_until prevents attempts
   Impact: Once locked, file becomes permanently inaccessible
   Fix Required: Reset failed_attempts when lock expires

8. DOWNLOAD COUNT INCREMENTED PREMATURELY
   Location: backend/transfers/views.py (Line 277)
   Issue: Count incremented after decryption but before actual download
   Impact: Download limit can be exhausted without successful downloads
   Fix Required: Move increment to after response is sent

9. IP LOCK CREATES PERMANENT LOCKOUT
   Location: backend/transfers/views.py (Lines 251-256)
   Issue: First downloader locks IP; if they fail and get locked, file is unusable
   Impact: One wrong password attempt can permanently lock a file
   Fix Required: Don't combine IP lock with failed attempt lock

10. SESSION OWNERSHIP NOT VALIDATED
    Location: backend/transfers/views.py (Line 79)
    Issue: Session ownership not checked during upload
    Impact: Any authenticated user can upload to another user's session
    Fix Required: Add filter: session = UploadSession.objects.get(session_id=session_id, user=request.user)

11. DUPLICATE PASSWORD REQUEST
    Location: backend/transfers/views.py (Lines 70, 87-91)
    Issue: Password requested and validated twice in online mode
    Impact: Inefficient code, confusing logic
    Fix Required: Remove duplicate password = request.data.get("password") at line 87

12. JWT TOKEN NEVER REFRESHED
    Location: frontend/src/services/api.js (Lines 7-12)
    Issue: No refresh token logic in interceptor
    Impact: Users logged out when access token expires (typically 5-15 minutes)
    Fix Required: Add response interceptor to refresh expired tokens

13. CHUNK RECONSTRUCTION GAPS IGNORED
    Location: backend/transfers/views.py (Lines 205-208)
    Issue: Missing chunks silently skipped during reconstruction
    Impact: Corrupted file reconstruction if chunks missing
    Fix Required: Validate all chunk indices 0 to total-1 exist before reassembly

14. NO FILE SIZE LIMITS
    Location: backend/transfers/views.py (upload_file)
    Issue: Entire file loaded into memory for encryption
    Impact: Large files will cause memory exhaustion and crashes
    Fix Required: Add MAX_FILE_SIZE validation and streaming encryption

15. BARE EXCEPTION HANDLER
    Location: backend/transfers/views.py (Line 263)
    Issue: except: catches ALL exceptions including system exits
    Impact: Can mask critical errors and prevent clean shutdown
    Fix Required: Change to except Exception as e:


================================================================================
MAJOR LOGICAL ISSUES
================================================================================

16. NO AUTOMATED CLEANUP
    Location: backend/transfers/management/commands/cleanup_expired_files.py
    Issue: Cleanup command exists but never scheduled
    Impact: Database fills with expired files indefinitely
    Fix Required: Add Celery periodic task or cron job

17. SESSION REUSE NOT PREVENTED
    Location: backend/transfers/models.py & views.py
    Issue: Session is_active never set to False after upload
    Impact: Same session can be used for multiple uploads (security issue)
    Fix Required: Set session.is_active = False after upload

18. INCOMPLETE AUDIT LOGGING
    Location: backend/transfers/views.py
    Issue: Only downloads logged, not uploads or session creations
    Impact: Incomplete audit trail for security investigations
    Fix Required: Add log_audit calls for all operations

19. NO USER ASSOCIATION IN AUDIT
    Location: backend/transfers/models.py (DownloadAudit)
    Issue: Audit logs don't track which user performed action
    Impact: Can't trace actions back to users
    Fix Required: Add user ForeignKey to DownloadAudit model

20. FILES NOT DELETED AFTER MAX DOWNLOADS
    Location: backend/transfers/views.py (download_online_file)
    Issue: File remains in DB after reaching 3 download limit
    Impact: Database bloat, expired files take up space
    Fix Required: Delete OnlineEncryptedFile after reaching limit

21. SECURITY THROUGH OBSCURITY
    Location: backend/transfers/views.py (error messages)
    Issue: Error messages too detailed ("IP locked", "Too many attempts")
    Impact: Attackers can enumerate file states
    Fix Required: Generic error messages ("Access denied")

22. CSRF VULNERABILITY
    Location: backend/transfers/views.py (file upload)
    Issue: Multipart/form-data uploads don't include CSRF token
    Impact: CSRF attacks possible on file upload
    Fix Required: Add CSRF exemption or handle token in multipart


================================================================================
MODERATE LOGICAL ISSUES
================================================================================

23. UTC TIMEZONE CONFUSION
    Location: backend/transfers/models.py, views.py
    Issue: All times in UTC but no timezone conversion for users
    Impact: Expiry times shown will confuse users in different timezones
    Fix Required: Convert to user's local timezone in responses

24. NO AUTO-LOGIN AFTER SIGNUP
    Location: frontend/src/pages/Signup.jsx
    Issue: User created but must manually login
    Impact: Poor user experience, extra step
    Fix Required: Auto-generate JWT tokens on signup and store them

25. NO LOGOUT FUNCTIONALITY
    Location: Frontend (missing)
    Issue: No way to programmatically logout
    Impact: Tokens stay in localStorage even after user wants to logout
    Fix Required: Add logout button that clears localStorage

26. ONETOONE CONSTRAINT LIMITS FUNCTIONALITY
    Location: backend/transfers/models.py (UploadedFile)
    Issue: OneToOne with UploadSession prevents multiple uploads
    Impact: Can't upload multiple files per session
    Fix Required: Change to ForeignKey if multiple uploads needed

27. NO METADATA VALIDATION IN QR RECONSTRUCTION
    Location: backend/transfers/views.py (reconstruct_from_zip)
    Issue: Metadata total_chunks not compared with actual chunks found
    Impact: Incomplete reconstructions accepted as valid
    Fix Required: Validate len(chunks) == metadata["total_chunks"]

28. NO CHUNK INDEX VALIDATION
    Location: backend/transfers/views.py (reconstruct_from_zip)
    Issue: No check for duplicate or missing chunk indices
    Impact: Corrupted reconstructions with duplicate/missing chunks
    Fix Required: Validate indices form complete sequence 0..n-1

29. NO FILE INTEGRITY VERIFICATION
    Location: backend/transfers/views.py (reconstruction)
    Issue: No checksum or hash validation after reconstruction
    Impact: Corrupted files delivered to users
    Fix Required: Include SHA256 hash in metadata and verify

30. ENVIRONMENT VARIABLE NOT USED
    Location: frontend/src/pages/OnlineDownload.jsx (Line 18)
    Issue: Hardcoded API URL instead of process.env.REACT_APP_API_URL
    Impact: URL inconsistency, harder to configure environments
    Fix Required: Use process.env.REACT_APP_API_URL

31. TOKEN VALIDATION INCOMPLETE
    Location: frontend/src/components/PrivateRoute.jsx
    Issue: Only checks token existence, not validity or expiry
    Impact: Expired tokens still grant access until API rejects
    Fix Required: Decode JWT and check expiry client-side

32. NO ERROR BOUNDARIES
    Location: Frontend (missing)
    Issue: No React error boundaries implemented
    Impact: Entire app crashes on component errors
    Fix Required: Add error boundary components

33. CORS HARDCODED IPS
    Location: backend/backend/settings.py (Lines 64-69)
    Issue: Specific IPs hardcoded instead of environment variables
    Impact: Won't work on different networks, must edit code
    Fix Required: Load from environment variables

34. NO REQUEST TIMEOUT CONFIGURATION
    Location: frontend/src/services/api.js
    Issue: No timeout set for axios requests
    Impact: Requests can hang indefinitely
    Fix Required: Add timeout: 30000 to axios config

35. PRINT STATEMENTS IN PRODUCTION CODE
    Location: backend/transfers/views.py (Line 239)
    Issue: print() used instead of proper logging
    Impact: Output lost in production, no log levels
    Fix Required: Replace with logger.error()

36. NO RATE LIMITING
    Location: Backend views
    Issue: No rate limiting on API endpoints
    Impact: Vulnerable to brute force and DoS attacks
    Fix Required: Add django-ratelimit or throttling

37. NO FILE TYPE VALIDATION
    Location: backend/transfers/views.py (upload_file)
    Issue: Any file type accepted without validation
    Impact: Malicious file types can be uploaded
    Fix Required: Add whitelist/blacklist for file types

38. SESSION ID EXPOSED IN RESPONSE
    Location: backend/transfers/views.py (create_session response)
    Issue: Session UUID sent to client and required for upload
    Impact: If intercepted, others can upload to that session
    Fix Required: Use signed session tokens instead of raw UUIDs


================================================================================
PRIORITY FIX ORDER (MOST CRITICAL FIRST)
================================================================================

IMMEDIATE (Must fix before any testing):
1. Fix signup authentication (Issue #1)
2. Fix download route protection (Issue #2)
3. Add password validation (Issue #3, #4, #5)
4. Fix session ownership validation (Issue #10)
5. Fix JWT refresh logic (Issue #12)

HIGH PRIORITY (Must fix before deployment):
6. Fix failed attempt deadlock (Issue #7)
7. Fix IP lock permanent lockout (Issue #9)
8. Add file size limits (Issue #14)
9. Fix bare exception handler (Issue #15)
10. Fix chunk reconstruction gaps (Issue #13)

MEDIUM PRIORITY (Should fix soon):
11. Add automated cleanup (Issue #16)
12. Prevent session reuse (Issue #17)
13. Fix download count timing (Issue #8)
14. Add complete audit logging (Issue #18)
15. Fix token expiry conflicts (Issue #6)

LOW PRIORITY (Fix when time allows):
16-38. Remaining issues (improve security, UX, and robustness)


================================================================================
END OF REPORT
================================================================================
